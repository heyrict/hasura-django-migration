{
  "name": "hasura-django-migration",
  "version": "0.0.1",
  "description": "hasura django migration example",
  "engines": {
    "npm": ">=5",
    "node": ">=8"
  },
  "author": "heyrict<github.com/heyrict>",
  "license": "MIT",
  "scripts": {
    "dev": "NODE_ENV=development node server",
    "start": "NODE_ENV=production node server",
    "keygen": "ssh-keygen -t rsa -f ./private.pem;ssh-keygen -f ./private.pem.pub -e -m pem > ./public.pem ; rm ./private.pem.pub"
  },
  "dependencies": {
    "express": "4.16.4",
    "fnv-plus": "^1.2.12",
    "jsonwebtoken": "^8.5.1",
    "pg": "^7.9.0",
    "rasha": "^1.2.5",
    "sequelize": "^5.15.1"
  },
  "devDependencies": {
    "prettier": "^1.16.4"
  }
}
